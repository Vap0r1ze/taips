{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": [ "../../scripts/build.sh" ],
      "outputs": [ "dist/**" ]
    },
    "publish": {
      "dependsOn": [ "build" ],
      "outputs": [ ]
    }
  }
}
